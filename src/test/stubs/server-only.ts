// Stub usado apenas em testes (vitest.config.ts) para permitir importar módulos marcados
// "server-only" fora do runtime do Next.js. Nunca é usado pela aplicação em produção — lá,
// o pacote real "server-only" continua em vigor normalmente.
export {};
