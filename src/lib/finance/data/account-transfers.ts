import type { AccountTransfer } from "@/lib/finance/types";

/** Nenhuma transferência real ocorreu ainda — o saldo do caixa físico vem só do fundo fixo informado. */
export const initialAccountTransfers: AccountTransfer[] = [];
